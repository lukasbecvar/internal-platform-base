framework:
    csrf_protection: true
    form:
        csrf_protection:
            enabled: true
            field_name: 'csrf_token'

when@test:
    framework:
        csrf_protection: false
        form:
            csrf_protection:
                enabled: false
